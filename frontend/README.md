# Frontend

This directory is intentionally left for a future web UI built with TypeScript and React. A three-dimensional visualization layer may be added later with Three.js or a comparable web-3D stack.
