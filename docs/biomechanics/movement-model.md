# Movement model

The movement model will eventually describe the athlete as a kinematic chain using segments, joints, and temporal events. The initial repository does not implement this model yet, but it is planned for later milestones.
